---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: centos7
      image: planetw/docker-centos7-ansible
      image_version: latest
      privileged: True
      required: True
    - name: ubuntu16.04
      image: planetw/docker-ubuntu1604-ansible
      image_version: latest
      privileged: True
      required: True
verifier:
  name: testinfra
