---
dependency:
  name: galaxy
driver:
  name: docker
  docker:
    containers:
      - name: centos7
        image: planetw/docker-centos7-ansible
        image_version: latest
        privileged: True
        # anti DBUS error
        required: True
verifier:
  name: testinfra
                    
                                    
